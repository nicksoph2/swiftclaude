You are implementing packet G1: Settings Key Registry and Schema-Driven Parsing.

## Context

Read these files first, in this order:
1. Documents/PROJECT_INDEX.md
2. Documents/IMPLEMENTATION_PLAN_V2.md (find the G1 section)
3. ClaudeConfigManager/Infrastructure/Parsers/SettingsParser.swift
4. ClaudeConfigManagerTests/Parsers/SettingsParserTests.swift
5. ClaudeConfigManager/Infrastructure/Resolver/ResolverModels.swift (for JSONValue type)

## What to build

The current SettingsParser has ~20 individually coded properties in SettingsDocumentValue. We need to scale to 70+ keys without adding a property for each one.

### Step 1: Create SettingsKeyDefinition and SettingsKeyRegistry

Create a new file `ClaudeConfigManager/Infrastructure/Parsers/SettingsKeyRegistry.swift`:

- `SettingsKeyDefinition` struct with: `key` (String), `expectedType` (enum: string, bool, int, stringArray, object, enumValue([String])), `category` (String), `description` (String), `priority` (String)
- `SettingsKeyRegistry` with a static `allKeys: [SettingsKeyDefinition]` covering all ~70 known keys from the gap analysis
- Group keys by category: "Model and AI Behavior", "Shell and Environment", "Permissions System", "Hooks System", "MCP Server Controls", "Plugin and Marketplace Controls", "Sandbox Configuration", "Worktree Settings", "UI and UX", "Git and Source Control", "Authentication", "Telemetry and Diagnostics", "Agents and Plans", "Updates and Experiments", "AWS Integration"
- Include a static `knownKeyNames: Set<String>` computed from allKeys
- Include `keys(inCategory:) -> [SettingsKeyDefinition]` helper

### Step 2: Refactor SettingsDocumentValue

Add to the existing SettingsDocumentValue:
- A `var allValues: [String: JSONValue]` dictionary that stores every parsed key-value pair
- Keep ALL existing named properties (env, permissions, hooks, etc.) working exactly as before
- The named properties should still be populated by the existing parse logic
- New/unknown keys that are in the registry get stored in allValues
- Unknown keys NOT in the registry still produce .info issues (existing behavior)

### Step 3: Update SettingsParser.parse()

After existing parse logic runs:
- Iterate over all top-level keys in the JSON
- For each key in the registry but not in knownTopLevelKeys, store it in allValues
- Validate the type matches the registry definition; produce .warning for mismatches
- Update knownTopLevelKeys to include all registry keys

### Step 4: Write tests

Add to SettingsParserTests:
- `testRegistryCoversAllDocumentedKeys`: verify SettingsKeyRegistry.allKeys.count >= 70
- `testRegistryKeyUniqueness`: verify no duplicate key names
- `testUnknownKeyInRegistryParsesToAllValues`: create a fixture with a key like "model" (which is in registry but not yet explicitly parsed), verify it appears in allValues
- `testTypeMismatchProducesWarning`: fixture with "model": 123 (should be string), verify warning issue

## Rules

- Do NOT change the signature or behavior of any existing property on SettingsDocumentValue
- All existing SettingsParserTests must still pass unchanged
- Run the full test suite: xcodebuild test -project ClaudeConfigManager.xcodeproj -scheme ClaudeConfigManager -destination 'platform=macOS' -quiet
- If tests fail, fix and re-run until green
- When done, create Documents/G1-handoff.md
