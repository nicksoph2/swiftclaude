Each directory is self-contained and designed for predictable navigation by both humans and AI agents.

---

## 🛠 Build System (XcodeGen)

This project uses **XcodeGen** to generate the `.xcodeproj` deterministically.

### Generate the project:

```sh
xcodegen generate
```
