# Resolver Fixtures - MCP

MCP resolver fixtures cover deterministic precedence, fallback when higher-precedence
entries are unusable, and environment-reference notes.

- `fallback_and_env_notes`: project-local invalid shape falls back to project server config.
