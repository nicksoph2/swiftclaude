# Resolver Fixtures - Agents and Skills

Agent/skill resolver fixtures cover precedence overrides and invalid entry behavior.

- `override_and_invalid_entries`: project overrides user identities and malformed/missing entries remain visible as invalid metadata rows.
