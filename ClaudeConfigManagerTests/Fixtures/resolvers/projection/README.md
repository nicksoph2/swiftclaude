# Resolver Fixtures - Session Projection

Session projection fixtures cover aggregate completeness behavior without UI code.

- `partial_settings_only`: only settings are available; all other families are explicitly missing.
