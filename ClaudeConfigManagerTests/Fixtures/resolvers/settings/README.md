# Resolver Fixtures - Settings

This family provides precedence and conflict cases.

- `override_project_wins`: higher-precedence project-local overrides user.
- `conflict_allow_deny_overlap`: permissions overlap conflict scenario.
